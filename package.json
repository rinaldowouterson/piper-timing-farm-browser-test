{
  "name": "piper-timing-farm-browser-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "dev:clean": "vite --force",
    "build": "vite build",
    "simulate": "npm run build && rm -rf dist_temp && mv dist dist_temp && mkdir -p dist/test-subfolder && mv dist_temp/* dist/test-subfolder/ && rm -rf dist_temp && npx http-server dist -p 8080",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "piper-timing-farm-browser": "^1.0.1"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  }
}
